% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decision_aids.R
\name{rank_probabilities}
\alias{rank_probabilities}
\title{Posterior rank probabilities for each group}
\usage{
rank_probabilities(
  fit,
  newdata = NULL,
  function_of = "median",
  times = NULL,
  tmax = NULL,
  ...
)
}
\arguments{
\item{fit}{A fitted `pooling_surv` object.}

\item{newdata}{Optional data frame with one row per group used to fix
covariates. If `NULL`, covariates are set to zero.}

\item{function_of}{Character; one of `"median"`, `"survival"` (alias
`"S_t"`), or `"rmst"`.}

\item{times}{Scalar time when `function_of = "survival"`.}

\item{tmax}{Positive horizon when `function_of = "rmst"`.}

\item{...}{Unused.}
}
\value{
A data frame with columns `group`, `rank`, and `prob`. Rank 1 is
  the largest summary value (best group).
}
\description{
For every posterior draw, the groups are ranked by a chosen survival
summary (median survival, survival at a fixed time, or RMST) and the
frequency of each rank is returned. Useful as a decision aid: the row of
the output for rank 1 gives the posterior probability that each group is
the best.
}
\examples{
set.seed(2719)
trial_data <- simulate_data(n = 90, K = 3, censoring_rate = 0.3)
fit <- pooling_surv(survival::Surv(time, event) ~ group,
                    data = trial_data,
                    iter = 300, warmup = 150, chains = 2)
rank_probabilities(fit)
}
