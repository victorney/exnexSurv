% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival.R
\name{rmst}
\alias{rmst}
\title{Restricted mean survival time (RMST) from an pooling_surv fit}
\usage{
rmst(fit, tmax = NULL, newdata = NULL, level = 0.95, grid_points = 400, ...)
}
\arguments{
\item{fit}{A fitted `pooling_surv` object.}

\item{tmax}{A positive scalar horizon up to which the RMST is computed.
Defaults to `max(fit$data$time)`.}

\item{newdata}{Optional data frame (one row gives one RMST).}

\item{level}{Credible-interval level (default `0.95`).}

\item{grid_points}{Resolution of the numerical quadrature (default `400`).}

\item{...}{Unused.}
}
\value{
A `data.frame` with columns `group`, `rmst`, `lower`, `upper`.
}
\description{
Computes the restricted mean survival time up to a horizon `tmax`:
\deqn{RMST(tmax) = \int_0^{tmax} S(t)\,dt.}
The integral is evaluated numerically (trapezoidal rule over a fine grid)
for every posterior draw, then summarised with posterior quantiles.
}
