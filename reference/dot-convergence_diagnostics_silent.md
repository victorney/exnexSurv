# Convergence diagnostics without warnings (used internally by summary)

Convergence diagnostics without warnings (used internally by summary)

## Usage

``` r
.convergence_diagnostics_silent(fit)
```
