# Run all chains concurrently on future workers

Run all chains concurrently on future workers

## Usage

``` r
.run_chains_parallel_pooling(
  cpp_data,
  priors,
  pooling,
  verbose,
  iter,
  warmup,
  chains,
  chain_seeds
)
```
