# Run multiple chains in parallel via PSOCK workers

Run multiple chains in parallel via PSOCK workers

## Usage

``` r
.run_chains_parallel_exnex(
  cpp_data,
  priors,
  iter,
  warmup,
  chains,
  parallel_chains,
  chain_seeds
)
```
