# Shared validation for common pooling_surv arguments

Shared validation for common pooling_surv arguments

## Usage

``` r
validate_args(
  priors,
  iter,
  warmup,
  thin,
  chains,
  parallel_chains,
  group_col,
  seed
)
```
